# Dependencies
node_modules/
client/node_modules/

# Production build
client/build/

# Lockfiles (installs resolve fresh — keeps the repo source-only)
package-lock.json
client/package-lock.json

# Environment
.env
.env.local

# Logs / OS
*.log
.DS_Store
